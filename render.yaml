services:
  - type: web
    name: layer3trading-site
    runtime: static
    plan: free
    branch: main
    autoDeploy: true
    buildCommand: echo "Static site ready"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
